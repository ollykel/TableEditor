// === TableProps ==============================================================
//
// Describes a table object
//
// =============================================================================

export default interface TableProps {
  id: number;
  name: string;
  width: number;
  height: number;
};
